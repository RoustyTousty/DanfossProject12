# To run the project, use:

```bash
dotnet run --project Presentation
```

Alternatively, you can navigate to Presentation folder and run:

```bash
dotnet run
```

# For now, Avalonia build process is disabled to make it easier to check if commands work through console.

To make Avalonia work again, uncomment lines 12-21 in Program.cs
